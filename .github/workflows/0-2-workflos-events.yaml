name: '02 - Eventos'
on: 
    - push
    - pull_request
    - schedule: cron '2 * * * *'
    - workflow_dispatch
jobs:
    echo-event:
      runs-on: ubuntu-latest
      steps:
        - name: ShowTrigger
          run: "Evento que desencadenó el workflow: ${{ github.event_name }}"